# ROADMAP — Generate Script Manual

Единый план по **всем скриптам** репозитория, вспомогательным файлам и документации. Детализация: **файл скрипта** → **логические блоки внутри файла** → **подпункты** (функции, сценарии UI, форматы выгрузки).

## Легенда статусов

| Статус | Значение |
|--------|----------|
| `[v]` | Сделано |
| `[w]` | В работе / требует доработки |
| `[ ]` | Не сделано |
| `[x]` | Отменено |

---

## 0. Репозиторий и инфраструктура (не `Script/`)

### 0.1 Корень проекта

- `[v]` **README.md** — описание структуры, ссылка на [Docs/Справочник_скрипты_HTTP_запросы_и_последовательность.md](Docs/Справочник_скрипты_HTTP_запросы_и_последовательность.md), перечень скриптов, история версий репозитория.
  - `[ ]` Периодическая сверка списка скриптов в README с фактическим содержимым `Script/*.js`.
  - `[ ]` При появлении нового скрипта — добавить строку в README, строку в справочник, подраздел в этом ROADMAP.

- `[v]` **ROADMAP.md** (этот файл) — декомпозиция по модулям и внутренним слоям скриптов.
  - `[ ]` По мере крупных изменений в скриптах — обновлять соответствующие подпункты и статусы.

- `[v]` **post_txt_sync.sh** — сборка текстовых копий для каталога `POST/` (суффикс `.txt` в имени файла).
  - `[ ]` Документировать в README любые новые шаблоны копируемых путей (если появятся).

- `[ ]` **post.txt** — шаблон запроса на пересборку `POST/`; держать в актуальном виде при смене процедуры.

### 0.2 Каталоги

- `[ ]` **Docs/** — профильная документация по каждому скрипту; сверка с кодом после каждого значимого PR.
  - `[ ]` Отдельный профильный `.md` для каждого нового `Script/*.js` (или явное решение объединить с существующим документом).

- `[ ]` **ToDo/** — черновики задач; перенос выполненного в ROADMAP/README и пометка в ToDo.

- `[ ]` **log/** — при появлении серверных/CLI-утилит с логами — согласовать формат имён и уровней с правилами проекта.

- `[x]` **POST/** — не в git; только локальная сборка через `post_txt_sync.sh`.

---

## 1. `Script/Profile_GP_LOAD_file.js`

**Сопутствующий файл документации:** [Docs/Скрипт_загрузка_профиля_герои.md](Docs/Скрипт_загрузка_профиля_герои.md)

### 1.1 Конфигурация и окружение

- `[ ]` Константы стендов/контуров (`PROFILE_ORIGINS`, ключи стенда и контура).
  - `[ ]` Сверка fallback при неизвестном `window.location.origin`.
  - `[ ]` Отображение на панели фактического `origin` и выбранной пары стенд/контур.

### 1.2 Разбор ввода

- `[ ]` Табельные из textarea, из `TAB_NUMS`, из файла `.txt` (`parseEmpIdsFromText` / нормализация).
  - `[ ]` Граничные случаи: пустой файл, нецифровой мусор, дубликаты ТН.

### 1.3 Сетевой слой

- `[ ]` `POST` профиля: тело `{ employeeNumber }`, заголовки для контура SIGMA (если актуально).
  - `[ ]` Обработка HTTP-ошибок и `success: false` в JSON.
  - `[ ]` Retry и паузы с панели.

### 1.4 Медиа и выгрузка

- `[ ]` Извлечение `photoData` / `photoDataKpk`, Blob, ссылки «Скачать».
- `[ ]` Итоговый JSON выгрузки и журнал на панели.

### 1.5 UI-панель

- `[ ]` Создание/удаление корня панели, блокировка параллельных прогонов.
- `[ ]` Три кнопки запуска в один ряд; закрытие и сброс состояния.

---

## 2. `Script/File_DB_Load_GP.js`

**Сопутствующий файл документации:** [Docs/Скрипт_выгрузка_файлов_gamification_File_DB_Load_GP.md](Docs/Скрипт_выгрузка_файлов_gamification_File_DB_Load_GP.md)

### 2.1 Конфигурация задач

- `[ ]` Таблица/массив задач `file-download`: пути, тела по умолчанию, подписи кнопок.
  - `[ ]` Основные три задачи + рейтинг + заказы — не расходиться с документом без отражения в Docs.

### 2.2 Окружение

- `[ ]` Выбор стенда × контура, автоопределение по `window.location.origin`.
- `[ ]` Индикатор `POST <origin>` на панели.

### 2.3 Пакетная отправка

- `[ ]` Последовательный режим и режим с перекрытием; паузы; чтение с панели.
- `[ ]` Поле даты наград (`dateFrom`) и подстановка в тело запросов, где требуется.

### 2.4 Обработка ответа

- `[ ]` Бинарное сохранение файла; ветка JSON с `success: false` при HTTP 200 — не сохранять файл, писать в журнал.

### 2.5 UI-панель

- `[ ]` Чекбоксы задач, «Отметить всё» / «Снять», сводка `(отмечено: N)`.
- `[ ]` `fileDlDetachPanelAndResetRuntime` при «Закрыть»; повторная вставка IIFE без конфликта имён.

---

## 3. `Script/AddressBook_export.js`

**Сопутствующий файл документации:** [Docs/Скрипт_AddressBook_export.md](Docs/Скрипт_AddressBook_export.md)

### 3.1 Origin и API

- `[ ]` `getAddressBookStandAndOrigin`, fallback `ADDRESSBOOK_ORIGINS.ALPHA`, базовый путь `/api/home`.

### 3.2 POST `employees/search`

- `[ ]` `fetchEmployeesSearch`: число vs строка в `searchText`, пагинация `pageToken`.
- `[ ]` `fetchAllSearchPages`: лимиты страниц, обработка повторяющегося токена, учёт `hits` в `data` и `data.body`.

### 3.3 GET `empInfoFull`

- `[ ]` `fetchEmpInfoFull(empId)` — UUID, не табельный номер.

### 3.4 Сценарий «Search → empInfoFull»

- `[ ]` Фаза 1: все поиски по списку; пауза «между запросами» между POST.
- `[ ]` Сохранение `addressbook_search_*.json` и CSV `что искали,employeeId` (строка на каждый hit, BOM).
- `[ ]` Пауза после всех Search перед первым GET.
- `[ ]` Фаза 2: уникальные UUID в порядке первого появления; паузы между GET.
- `[ ]` Итоговый `addressbook_empInfoFull_*.json` со ссылками на файлы фазы 1.

### 3.5 Сценарии «Только Search» / «Только empInfoFull»

- `[ ]` Разбор входа по режиму табельный / строка поиска; только JSON выгрузки.

### 3.6 UI-панель

- `[ ]` Режимы разбора, шесть кнопок (поле + файл), журнал, поля пауз.

### 3.7 Вспомогательные функции

- `[ ]` `pickEmployeeIdsFromSearchData`, `collectEmployeeIdsFromSearchPagesInHitOrder`, `uniqueEmployeeIdsFirstOccurrence`.
- `[ ]` `downloadJson`, `downloadText`, `escapeCsvField`.

---

## 4. `Script/Parameters_Actual_Export.js`

**Сопутствующий файл документации:** [Docs/Скрипт_выгрузка_актуальных_параметров_Parameters_Actual_Export.md](Docs/Скрипт_выгрузка_актуальных_параметров_Parameters_Actual_Export.md)

### 4.1 Окружение и вкладки

- `[ ]` Стенд × контур, автоопределение, индикатор `POST <origin>`.
- `[ ]` Три вкладки: выгрузка, создание (`param-create`), редактирование (`param-update`).

### 4.2 Общая загрузка справочников

- `[ ]` Кнопка «Загрузить параметры»; кэши ACTUAL, кодов, типов, `businessBlock`, карты связей.
  - `[ ]` Различие логики списков для вкладки «Создание» vs «Редактирование» (шаг 1 / шаг 2).

### 4.3 Вкладка выгрузки

- `[ ]` Цепочка `status` → `objectIds` → детализация; паузы; итоговый JSON.

### 4.4 Вкладка создания

- `[ ]` Форма и файл; валидация; модальное подтверждение payload.
- `[ ]` Fallback-списки `businessBlock` и типов.

### 4.5 Вкладка редактирования

- `[ ]` Фильтры `businessBlock` / `parameterType` / `parameterCode`; datalist и picker (при наличии в коде).
- `[ ]` Проверка комбинации `objectId + parameterCode + version`; сравнение «было/стало»; сокращённый payload при смене только `status`.
- `[ ]` Кнопка «Сформировать шаблон Payload» (`🧩`): объединение ACTUAL + ARCHIVE, детализация по id.

### 4.6 Разбор JSON из файла

- `[ ]` Нормализация висячих запятых; диагностика позиции ошибки.

### 4.7 UI

- `[ ]` Верхняя строка действий по контексту вкладки; модальные окна широкого формата.

---

## 5. `Script/Tournament_LeadersForAdmin.js`

**Сопутствующий файл документации:** [Docs/Скрипт_турниры_leadersForAdmin.md](Docs/Скрипт_турниры_leadersForAdmin.md)

### 5.1 Конфигурация

- `[ ]` `TOURNAMENT_BASE`, ключи стендов и контуров, автоопределение окружения.
- `[ ]` Дефолтный текст поля кодов турниров.

### 5.2 Сбор кодов

- `[ ]` Textarea, `.txt`, CSV с выбором колонки и фильтром статусов по кнопкам SHEDULE/LIST.

### 5.3 GET `leadersForAdmin`

- `[ ]` Формирование URL; обработка успеха и вложенных ошибок (`body.tournament.error`).
- `[ ]` Запись «0 участников»; отказ от скачивания пустого `{}`.

### 5.4 Экспорт

- `[ ]` JSON; CSV с общим выбором колонок (`<select>` + «Другой…»).
- `[ ]` Пауза между турнирами; префикс имени файла.

### 5.5 UI-панель

- `[ ]` IIFE, журнал, блокировка повторного запуска.

---

## 6. `Script/UI_AutoTest.js`

**Профильный документ в `Docs/`:** отсутствует (краткое описание — в [Справочник](Docs/Справочник_скрипты_HTTP_запросы_и_последовательность.md), раздел 6).

### 6.1 Назначение

- `[ ]` Последовательный клик по фиксированному списку `MENU_HREFS` (основное и admin-меню).
- `[ ]` Ожидание загрузки (`document.readyState`, смена URL, таймаут); паузы `STEP_DELAY_MS`.

### 6.2 Логирование

- `[ ]` Консоль: OK / НЕ OK по шагам, итоговые счётчики.

### 6.3 Дальнейшее развитие (опционально)

- `[ ]` Вынести `MENU_HREFS` и тайминги на читаемый конфиг (если появится общий `config.json` для браузерных скриптов — согласовать формат).
- `[ ]` При необходимости — отдельный `Docs/Скрипт_UI_AutoTest.md` с таблицей href и ограничениями SPA.

---

## 7. `Script/UI_AutoTest_LinksCrawler.js`

**Сопутствующий файл документации:** [Docs/Скрипт_UI_AutoTest_LinksCrawler.md](Docs/Скрипт_UI_AutoTest_LinksCrawler.md)

### 7.1 Жизненный цикл панели

- `[ ]` IIFE; удаление предыдущей панели; компактный режим на время этапа; кнопка «Остановить».

### 7.2 Этап 1

- `[ ]` Скан ссылок на текущей странице; ручной выбор; запуск кликов по `<a>`.

### 7.3 Этапы 2..N

- `[ ]` Автосбор дочерних ссылок с успешных узлов; группировка по родителю на этапах `>1`; каскад чекбоксов.

### 7.4 Статусы и дерево

- `[ ]` `OK` / `FAIL` / `SKIPPED`; вывод дерева в консоль; при остановке — дерево проблемных узлов.

### 7.5 Лимиты и устойчивость

- `[ ]` Лимиты на этап/родителя/рендер UI; кнопка сброса лимитов.
- `[ ]` Лог в файл (File System Access API), восстановление из `sessionStorage`.
- `[ ]` Прокрутка панели без прокрутки фоновой страницы.

### 7.6 Документация

- `[ ]` Синхронизация длинного сценария в `Скрипт_UI_AutoTest_LinksCrawler.md` с фактическим кодом при каждом изменении поведения этапов.

---

## 8. Сводная таблица «скрипт ↔ документ ↔ раздел ROADMAP»

| Файл `Script/` | Основной документ `Docs/` | Раздел ROADMAP |
|----------------|----------------------------|----------------|
| `Profile_GP_LOAD_file.js` | Скрипт_загрузка_профиля_герои.md | § 1 |
| `File_DB_Load_GP.js` | Скрипт_выгрузка_файлов_gamification_File_DB_Load_GP.md | § 2 |
| `AddressBook_export.js` | Скрипт_AddressBook_export.md | § 3 |
| `Parameters_Actual_Export.js` | Скрипт_выгрузка_актуальных_параметров_Parameters_Actual_Export.md | § 4 |
| `Tournament_LeadersForAdmin.js` | Скрипт_турниры_leadersForAdmin.md | § 5 |
| `UI_AutoTest.js` | — (справочник § 6) | § 6 |
| `UI_AutoTest_LinksCrawler.js` | Скрипт_UI_AutoTest_LinksCrawler.md | § 7 |

**Общий HTTP-справочник:** [Docs/Справочник_скрипты_HTTP_запросы_и_последовательность.md](Docs/Справочник_скрипты_HTTP_запросы_и_последовательность.md)

---

*Первый пункт плана в смысле проектных правил: документ ROADMAP создан и принят к ведению — статусы `[v]` в § 0.1 и § 8 отражают фиксацию структуры; детальные работы по скриптам — в подпунктах со статусом `[ ]` до явного выполнения.*
